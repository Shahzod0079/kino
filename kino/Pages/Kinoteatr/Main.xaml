<Page x:Class="kino.Pages.Kinoteatr.Main"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:kino.Pages.Kinoteatr"
      xmlns:items="clr-namespace:kino.Pages.Kinoteatr.Items"  
    mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Main" Background="White">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="1" Background="#F0F0F0" Padding="10" Margin="5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="50"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="50"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Label Grid.Column="0" Content="Название:" VerticalAlignment="Center"/>
                <TextBox x:Name="txtName" Grid.Column="1" Margin="5" Height="25"/>

                <Label Grid.Column="2" Content="Залы от:" VerticalAlignment="Center" Margin="10,0,0,0"/>
                <TextBox x:Name="txtMinZal" Grid.Column="3" Width="50" Margin="5" Height="25"/>
                <Label Grid.Column="4" Content="до" VerticalAlignment="Center"/>
                <TextBox x:Name="txtMaxZal" Grid.Column="5" Width="50" Margin="5" Height="25"/>

                <Button x:Name="btnClear" Grid.Column="6" Content="Сброс" 
                        Click="ClearFilter" Height="25" Width="60" Background="#FFE68918" Margin="5"/>
                <Button x:Name="btnApply" Grid.Column="7" Content="Применить" 
                        Click="ApplyFilter" Height="25" Width="80" Background="#FF73E618" Margin="5"/>
            </Grid>
        </Border>

        <ScrollViewer Grid.Row="2" Margin="5">
            <StackPanel x:Name="parent">

            </StackPanel>
        </ScrollViewer>

        <Button Grid.Row="3" x:Name="btnAdd" Content="Добавить" 
                Click="AddRecord" Height="30" Width="100" 
                Background="#FF73E618" Margin="5" HorizontalAlignment="Right"/>
    </Grid>
</Page>